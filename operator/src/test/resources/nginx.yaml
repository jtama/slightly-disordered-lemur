apiVersion: v1
kind: Pod
metadata:
  labels:
    run: pod
  name: pod
  namespace: lemure-dev
spec:
  containers:
    - image: nginx
      name: pod
  dnsPolicy: ClusterFirst
  restartPolicy: Always

---

apiVersion: v1
kind: Pod
metadata:
  labels:
    run: pod
  name: pod2
  namespace: lemure-dev
spec:
  containers:
    - image: nginx
      name: pod2
  dnsPolicy: ClusterFirst
  restartPolicy: Always


---

apiVersion: v1
kind: Pod
metadata:
  labels:
    run: pod
  name: pod3
  namespace: lemure-dev
spec:
  containers:
    - image: nginx
      name: pod3
  dnsPolicy: ClusterFirst
  restartPolicy: Always
